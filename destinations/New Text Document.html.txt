<!DOCTYPE html>
<html lang="es">
<head>
    <!-- =============================================
         META TAGS - EDITAR PARA CADA POST
         ============================================= -->
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <!-- EDITAR: Descripción del post para SEO -->
    <meta name="description" content="Breve descripción del post para motores de búsqueda">
    <!-- EDITAR: Título del post -->
    <title>Mi Sitio Web - Título del Post</title>
    <link rel="icon" type="image/x-icon" href="../assets/favicon.ico">

    <!-- Fuentes -->
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Mochiy+Pop+One&display=swap" rel="stylesheet">

    <!-- =============================================
         ESTILOS CSS (MISMOS QUE BLOG.HTML)
         ============================================= -->
    <style>
        /* === VARIABLES (COPIAR DE BLOG.HTML) === */
        :root {
            --bg-color: #fafafa; --text-color: #000000;
            --navbar-bg: #f5f5f5; --navbar-text: #000000;
            --btn-bg: #ff7227; --btn-text: #ffffff;
            --card-bg: #ffffff; --card-shadow: rgba(0,0,0,0.08);
            --footer-bg: #1a1a1a; --footer-text: #ffffff;
            --accent-color: #ff7227;
        }
        body.dark-mode {
            --bg-color: #121212; --text-color: #ffffff;
            --navbar-bg: #1a1a1a; --navbar-text: #ffffff;
            --card-bg: #1e1e1e; --card-shadow: rgba(0,0,0,0.3);
            --footer-bg: #0a0a0a; --footer-text: #ffffff;
        }

        /* === RESET Y BASE === */
        * { box-sizing: border-box; margin: 0; padding: 0; }
        body {
            font-family: 'Mochiy Pop One', sans-serif;
            background-color: var(--bg-color);
            color: var(--text-color);
            line-height: 1.8;
            transition: background-color 0.3s ease, color 0.3s ease;
            min-height: 100vh;
            display: flex;
            flex-direction: column;
        }
        a { text-decoration: none; color: inherit; transition: 0.3s; }
        img { max-width: 100%; display: block; }
        ul { list-style: none; }

        /* === NAVBAR (COPIAR DE BLOG.HTML) === */
        .navbar {
            position: fixed; top: 20px; left: 50%; transform: translateX(-50%);
            width: calc(100% - 40px); max-width: 1200px;
            background: var(--navbar-bg); z-index: 1000; height: 70px;
            display: flex; align-items: center; justify-content: space-between;
            padding: 0 30px; border-radius: 50px;
            box-shadow: 0 4px 20px rgba(0,0,0,0.1);
        }
        .nav-links { display: flex; gap: 15px; flex-wrap: wrap; justify-content: center; }
        .nav-links a {
            color: var(--navbar-text); font-weight: bold; text-transform: uppercase;
            font-size: 13px; padding: 8px 16px; border-radius: 25px;
        }
        .nav-links a:hover { background: var(--btn-bg); color: var(--btn-text); }
        
        .theme-toggle {
            background: transparent; border: 2px solid var(--navbar-text);
            color: var(--navbar-text); width: 45px; height: 45px; border-radius: 50%;
            cursor: pointer; display: flex; align-items: center; justify-content: center;
            font-size: 20px; margin-left: 15px;
        }
        .theme-toggle .icon-sun { display: none; }
        .theme-toggle .icon-moon { display: block; }
        body.dark-mode .theme-toggle .icon-sun { display: block; }
        body.dark-mode .theme-toggle .icon-moon { display: none; }

        /* === HERO DEL POST === */
        .post-hero {
            position: relative; height: 50vh; min-height: 350px;
            margin-top: 0; overflow: hidden;
        }
        .post-hero img {
            width: 100%; height: 100%; object-fit: cover;
            position: absolute; z-index: 1;
        }
        .post-hero::after {
            content: ''; position: absolute; top: 0; left: 0;
            width: 100%; height: 100%;
            background: linear-gradient(180deg, rgba(0,0,0,0.2) 0%, rgba(0,0,0,0.6) 100%);
            z-index: 2;
        }

        /* === CONTENIDO DEL POST === */
        .post-content {
            max-width: 800px;
            margin: -60px auto 60px;  /* Superpone ligeramente al hero */
            padding: 40px 30px;
            background: var(--card-bg);
            border-radius: 30px 30px 0 0;
            box-shadow: 0 -4px 20px var(--card-shadow);
            position: relative;
            z-index: 3;
            flex-grow: 1;
        }

        .post-meta {
            display: flex;
            gap: 20px;
            margin-bottom: 25px;
            padding-bottom: 20px;
            border-bottom: 2px solid var(--card-shadow);
            flex-wrap: wrap;
        }
        .post-date {
            display: flex;
            align-items: center;
            gap: 5px;
            opacity: 0.8;
            font-size: 0.95rem;
        }
        .post-tags {
            display: flex;
            gap: 8px;
            flex-wrap: wrap;
        }
        .post-tag {
            padding: 4px 12px;
            background: var(--accent-color);
            color: var(--btn-text);
            border-radius: 15px;
            font-size: 0.85rem;
        }

        .post-title {
            font-size: clamp(1.8rem, 4vw, 2.5rem);
            margin-bottom: 25px;
            line-height: 1.3;
        }

        .post-body {
            font-size: 1.05rem;
            line-height: 1.9;
        }
        .post-body p { margin-bottom: 20px; }
        .post-body h2 {
            font-size: 1.6rem;
            margin: 35px 0 15px;
            color: var(--accent-color);
        }
        .post-body h3 {
            font-size: 1.3rem;
            margin: 25px 0 12px;
        }
        .post-body ul, .post-body ol {
            margin: 15px 0 15px 25px;
        }
        .post-body li { margin-bottom: 8px; }
        .post-body img {
            border-radius: 15px;
            margin: 25px auto;
            box-shadow: 0 4px 15px var(--card-shadow);
        }
        .post-body a {
            color: var(--accent-color);
            text-decoration: underline;
        }
        .post-body a:hover { opacity: 0.8; }
        .post-body blockquote {
            border-left: 4px solid var(--accent-color);
            padding: 15px 25px;
            margin: 25px 0;
            background: rgba(255,114,39,0.1);
            border-radius: 0 12px 12px 0;
            font-style: italic;
        }

        /* === NAVEGACIÓN ENTRE POSTS === */
        .post-navigation {
            display: flex;
            justify-content: space-between;
            padding: 30px;
            border-top: 2px solid var(--card-shadow);
            margin-top: 40px;
        }
        .post-nav-link {
            display: flex;
            flex-direction: column;
            gap: 8px;
            max-width: 45%;
        }
        .post-nav-link.prev { align-items: flex-start; }
        .post-nav-link.next { align-items: flex-end; }
        .post-nav-label {
            font-size: 0.85rem;
            opacity: 0.7;
        }
        .post-nav-title {
            font-weight: bold;
            color: var(--accent-color);
        }
        .post-nav-title:hover { opacity: 0.8; }

        /* === FOOTER === */
        footer {
            background: var(--footer-bg);
            color: var(--footer-text);
            text-align: center;
            padding: 40px 20px;
            margin: 0 20px 20px;
            border-radius: 30px;
            font-size: 0.95rem;
        }

        /* === RESPONSIVE === */
        @media (max-width: 768px) {
            .navbar {
                top: 10px; width: calc(100% - 20px);
                flex-direction: column; height: auto; padding: 15px; border-radius: 30px;
            }
            .nav-links { margin: 15px 0; gap: 10px; }
            .nav-links a { padding: 6px 12px; font-size: 11px; }
            .theme-toggle { margin: 10px auto 0; }
            .post-hero { height: 40vh; min-height: 280px; }
            .post-content {
                margin: -40px 10px 0;
                padding: 30px 20px;
                border-radius: 20px 20px 0 0;
            }
            .post-navigation {
                flex-direction: column;
                gap: 20px;
                align-items: center;
            }
            .post-nav-link { max-width: 100%; align-items: center !important; text-align: center; }
            footer { margin: 40px 10px 10px; border-radius: 20px; }
        }
    </style>
</head>
<body>

    <!-- NAVBAR (MISMA QUE BLOG.HTML) -->
    <nav class="navbar">
        <ul class="nav-links">
            <li><a href="../index.html">Inicio</a></li>
            <li><a href="../about.html">Nosotros</a></li>
            <li><a href="../index.html#servicios">Servicios</a></li>
            <li><a href="../index.html#galeria">Galería</a></li>
            <li><a href="../contact.html">Contacto</a></li>
            <li><a href="../blog.html">Blog</a></li>
        </ul>
        <button class="theme-toggle" id="themeToggle" aria-label="Cambiar tema">
            <span class="icon-sun">☀️</span><span class="icon-moon">🌙</span>
        </button>
    </nav>

    <!-- HERO CON IMAGEN DE PORTADA -->
    <section class="post-hero">
        <!-- EDITAR: Ruta a la imagen de portada del post -->
        <img src="../assets/posts/post-001-cover.jpg" alt="Portada del post">
    </section>

    <!-- CONTENIDO DEL POST -->
    <article class="post-content">
        <!-- META: Fecha y Tags -->
        <div class="post-meta">
            <!-- EDITAR: Fecha del post (formato ISO para accesibilidad) -->
            <time class="post-date" datetime="2026-06-01">📅 1 jun 2026</time>
            <!-- EDITAR: Tags del post -->
            <div class="post-tags">
                <span class="post-tag">anuncio</span>
                <span class="post-tag">blog</span>
                <span class="post-tag">novedades</span>
            </div>
        </div>

        <!-- TÍTULO DEL POST -->
        <!-- EDITAR: Título principal -->
        <h1 class="post-title">✨ Mi Primer Post en el Blog ✨</h1>

        <!-- CUERPO DEL POST -->
        <div class="post-body">
            <!-- EDITAR: Contenido del post (puedes usar HTML) -->
            <p>¡Bienvenidos a nuestro nuevo espacio de contenido! 🎉</p>
            
            <p>En este blog compartiremos:</p>
            <ul>
                <li>📚 Tutoriales paso a paso</li>
                <li>🎨 Reflexiones sobre diseño y desarrollo</li>
                <li>🚀 Novedades de nuestros proyectos</li>
                <li>💡 Consejos y mejores prácticas</li>
            </ul>

            <h2>¿Qué puedes esperar?</h2>
            <p>Contenido fresco, útil y creado con cariño. Nuestro objetivo es que cada lectura te aporte valor y te inspire en tus propios proyectos.</p>

            <blockquote>
                "La mejor manera de predecir el futuro es crearlo." — Peter Drucker
            </blockquote>

            <p>¡Gracias por visitarnos y no olvides suscribirte para no perderte ninguna entrada! ✨</p>

            <!-- Ejemplo de imagen dentro del contenido -->
            <!-- <img src="../assets/posts/post-001-inline.jpg" alt="Descripción de la imagen"> -->
        </div>

        <!-- NAVEGACIÓN: Anterior / Siguiente (opcional) -->
        <nav class="post-navigation">
            <!-- EDITAR: Enlace al post anterior (si existe) -->
            <!--
            <a href="post-anterior.html" class="post-nav-link prev">
                <span class="post-nav-label">← Post anterior</span>
                <span class="post-nav-title">Título del post anterior</span>
            </a>
            -->
            
            <!-- EDITAR: Enlace al post siguiente (si existe) -->
            <!--
            <a href="post-siguiente.html" class="post-nav-link next">
                <span class="post-nav-label">Post siguiente →</span>
                <span class="post-nav-title">Título del post siguiente</span>
            </a>
            -->
        </nav>
    </article>

    <!-- FOOTER -->
    <footer>
        <p>&copy; 2026 Mi Sitio Web. Todos los derechos reservados.</p>
    </footer>

    <!-- JAVASCRIPT: TEMA DÍA/NOCHE -->
    <script>
        const themeToggle = document.getElementById('themeToggle');
        const body = document.body;
        const savedTheme = localStorage.getItem('theme');
        if (savedTheme === 'dark') body.classList.add('dark-mode');
        themeToggle.addEventListener('click', () => {
            body.classList.toggle('dark-mode');
            localStorage.setItem('theme', body.classList.contains('dark-mode') ? 'dark' : 'light');
        });
    </script>
</body>
</html>